plugins {
    // Keep plugin versions in sync with gradle/libs.versions.toml (or the version catalog)
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "2.0.20" apply false
}